[package]
name = "field-projection-design"
version = "0.1.0"
edition = "2024"

[dependencies]
macros = { path = "./macros" }

[features]
default = ["place-wrappers"]
place-wrappers = []

[dev-dependencies]
pin-project = "1.1.13"
